{
  "name": "rummikub-project",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prod": "docker compose down && docker compose up --build -d && ngrok http 5173 --pooling-enabled & ngrok http 8080 --pooling-enabled",
    "dev": "docker compose down && docker compose up --build -d",
    "down": "docker compose down",
    "start-backend": "cd backend && go run main.go",
    "start-frontend": "cd frontend && npm run dev -- --host"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs"
}
